# MemFS From Scratch

This directory is the guided rewrite version of `memfs_refactor`.

The framework, module layout, helper types, and tests are prepared first. Core file-system logic is intentionally left as `todo!()` so it can be implemented step by step.
